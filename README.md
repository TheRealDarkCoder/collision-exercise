# neomon

A tiny synthetic vitals feed.

## Run it

```bash
python monitor.py
```

Requires Python 3.8+. No dependencies. All data is synthetic.

## Your task

Each of you has been assigned **one issue** (see the Issues tab).

The only instruction: **get your change into `main`.**

The clock starts now.
